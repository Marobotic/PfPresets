<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <AssemblyName>PfPresets</AssemblyName>
        <RootNamespace>PfPresets</RootNamespace>
        <Version>3.2.3.0</Version>
        <FileVersion>3.2.3.0</FileVersion>
        <AssemblyVersion>3.2.3.0</AssemblyVersion>
        <TargetFramework>net10.0-windows</TargetFramework>
        <Platforms>x64</Platforms>
        <Nullable>enable</Nullable>
        <LangVersion>latest</LangVersion>
        <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
        <ProduceReferenceAssembly>false</ProduceReferenceAssembly>
        <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
        <CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>
    </PropertyGroup>

    <!--
      Phase 2 community ratings. Kept behind a compile flag because the official Dalamud
      repo's rules bar plugins that collect and share data about other players: a build
      with EnableRatings=false contains none of the rating code and stays submittable
      there, while the third-party repo ships the full thing.

      Off by default so an unqualified `dotnet build` produces the conservative build.
      Turn it on explicitly with -p:EnableRatings=true; build.sh does that for you
      unless you pass its no-ratings switch.
    -->
    <PropertyGroup>
        <EnableRatings Condition="'$(EnableRatings)' == ''">false</EnableRatings>
    </PropertyGroup>
    <PropertyGroup Condition="'$(EnableRatings)' == 'true'">
        <DefineConstants>$(DefineConstants);PFP_RATINGS</DefineConstants>
    </PropertyGroup>

  <ItemGroup>
    <Compile Remove="scratch\**" />
    <Content Remove="scratch\**" />
    <None Include="scratch\**" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="DalamudPackager" Version="11.0.0" />
    <Reference Include="Dalamud">
      <HintPath>$(appdata)\XIVLauncher\addon\Hooks\dev\Dalamud.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="FFXIVClientStructs">
      <HintPath>$(appdata)\XIVLauncher\addon\Hooks\dev\FFXIVClientStructs.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="InteropGenerator.Runtime">
      <HintPath>$(appdata)\XIVLauncher\addon\Hooks\dev\InteropGenerator.Runtime.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="Lumina">
      <HintPath>$(appdata)\XIVLauncher\addon\Hooks\dev\Lumina.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="Lumina.Excel">
      <HintPath>$(appdata)\XIVLauncher\addon\Hooks\dev\Lumina.Excel.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="Dalamud.Bindings.ImGui">
      <HintPath>$(appdata)\XIVLauncher\addon\Hooks\dev\Dalamud.Bindings.ImGui.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="Newtonsoft.Json">
      <HintPath>$(appdata)\XIVLauncher\addon\Hooks\dev\Newtonsoft.Json.dll</HintPath>
      <Private>false</Private>
    </Reference>
  </ItemGroup>
  <ItemGroup>
    <!-- Roboto (Apache 2.0), embedded for the profile card's score. Dalamud ships Noto and the
         game fonts, neither of which is this, and the number is the one place the plugin sets
         type large enough for the face to matter. -->
    <EmbeddedResource Include="Data\Fonts\Roboto.ttf" />
  </ItemGroup>

</Project>
